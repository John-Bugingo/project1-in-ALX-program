#include <stdio.h>
/**
 * print_my_name - a function that prints additin of two numbers
 * @n: number to count the numbers in an array
 * Return: arrays counter
 */
int){
	int array[54] = {2, 4, 3, 8, 0, 23, 4, 5, 7, 6};
	int i, count = 0;

	for (i = 0; i < n; i++)
	{
		count += i;
	}
	return (count);
}
/**
 * main - this returns integer
 *Return: 0
 */
int main(void)
{
	print_my_name(5);
	return (0);
}
